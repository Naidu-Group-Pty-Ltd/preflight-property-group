# AML Traceability Matrix (report §23)

Maps requirements → build artifacts → tests. Kept in sync with `acceptance-scenarios.md` and Launch Ops.

| Requirement | Phase | Tables | Edge fn | UI surface | Acceptance |
| --- | --- | --- | --- | --- | --- |
| Client intake + consent | 3 | `aml.cases`, `aml.consents` | `aml-cases`, `aml-client-portal` | Intake Queue, Client Portal | AS-01 |
| Verification (IDV) | 4 | `aml.verifications` | `aml-verification`, `aml-provider-webhook` | Verification | AS-01 |
| Screening (PEP / sanctions / adverse) | 4 | `aml.screenings`, `aml.matches` | `aml-provider-webhook` | Screening | AS-02 |
| Risk model | 5 | `aml.risk_scores` | `aml-risk` | Risk | AS-03 |
| Beneficial owners | 6 | `aml.counterparties`, `aml.counterparty_requests` | `aml-entities` | Counterparty | AS-04 |
| Finance handoff | 7 | `aml.handoff_tokens` (existing finance patterns) | `aml-finance` | Finance | AS-05 |
| Transactions | 8 | `aml.transactions` | `aml-transactions` | Transactions | AS-06 |
| Monitoring / rescreen | 9 | `aml.monitoring_rules`, `aml.alerts`, `aml.edd_cases` | `aml-monitoring` | Monitoring | AS-07 |
| AUSTRAC reporting | 10 | `aml.reports`, `aml.report_versions` | `aml-reporting` | AUSTRAC Reporting | AS-08 |
| Records + privacy | 11 | `aml.retention_schedules`, `aml.legal_holds`, `aml.retention_scans`, `aml.privacy_requests` | `aml-records` | Records & Privacy | AS-09 |
| Tenant / white-label / providers | 12 | `aml.tenant_settings`, `aml.plan_tiers`, `aml.provider_configs`, `aml.provider_metrics_daily` | `aml-tenant` | Configuration | AS-10 |
| Step-up + AI guardrail | 13 | `aml.step_up_challenges`, `aml.step_up_sessions`, `aml.ai_action_approvals`, `aml.release_gates`, `aml.resilience_drills` | `aml-step-up`, `aml-ai-guardrail`, `aml-release-gate`, `aml-resilience` | Governance | AS-11 |
| Rollout + acceptance + risk | 14 | `aml.rollout_stage_history`, `aml.acceptance_scenarios`, `aml.risk_register` | `aml-launch-ops` | Launch Ops | AS-12 |
| Non-AML regression | X | n/a | n/a | Command Centre / Client Portal / Finance Portal | AS-13 |
| Canonical workflow dimensions + activation contract + provenance (V2 directive Phase 1) | V2-1 | `aml.cases` (dimension/activation columns), `aml.workflow_dimension_migrations`, `aml.field_provenance` | `aml-cases`, `aml-finance` (`limited_status` finance-safe), `aml-client-portal` (portal-safe status) | Finance portal `LimitedAmlStatusCard`; contracts in `src/lib/aml/caseDimensions.ts` | `caseDimensions.test.ts`; docs/aml/canonical-contracts.md §7 |
| Operational Compliance Home + production-surface disclosure rules (V2 directive Phase 2) | V2-2 | n/a (uses existing case/monitoring/discrepancy reads) | `aml-cases` list, `aml-monitoring` summary, `aml-finance` list_discrepancies | `AmlComplianceHomeV3` (priority queue, next-best-action, operational metrics), `AmlCases`/`AmlOverview`/`AmlGuard` commercial copy | `amlUiDisclosure.source.test.ts` |
| Full-page case workspace + commercial register + restricted manual creation (V2 directive Phase 3) | V2-3 | existing case tables; `metadata.creation_exception` on exception cases | `aml-cases` (create restricted to MLRO exception; staff document/request ops) | `/admin/aml/cases/:caseId` (`AmlCaseWorkspace`: header, grouped nav, progress rail, action panel, Overview, Documents & Evidence, Requests), `AmlCases` table + saved views | `caseDimensions.test.ts` (rail), `amlPortalContracts.test.ts` (creation exception) |
| Client record integration: persistent AML summary + client picker (V2 directive Phase 4) | V2-4 | reads `aml.cases`, `aml.document_requirements`, `aml.client_requests` | `aml-cases` `client_summary` (read-only, client-scoped) | `ClientAmlSummaryCard` on the client Overview tab (pre/post-activation states, duplicate-case prevention, deep links, gate visibility); `ActivateClientDialog` client search picker replaces raw-UUID entry; plain-English activation-timing labels | `amlPortalContracts.test.ts` (client-summary scoping, no-UUID-entry) |
| Conditional versioned Client Portal onboarding + related parties (V2 directive Phase 5) | V2-5 | `aml.questionnaire_responses` (sections `entity_details`, `related_parties`); snapshot gains `questionnaire_version` + `applicable_sections` | `aml-client-portal` (server-driven `applicableSections`, v2 engine, section-completeness gate on submit) | `PortalAml` dynamic server-driven stepper; `EntityDetailsForm` (company/trust/SMSF/LRBA), `RelatedPartiesForm` (repeatable party rows: co-purchasers, directors, trustees, BOs, representatives, donors, lenders) | `amlPortalContracts.test.ts` (engine version, structure mapping, catalogue validation, submit gate) |
| Parties, ownership & evidence: questionnaire reconciliation + case ownership surface (V2 directive Phase 6) | V2-6 | `aml.entities`, `aml.beneficial_owners`, `aml.authorised_representatives`, `aml.entity_case_links` (existing engine), `aml.field_provenance` (append-first source record) | `aml-entities` (`import_from_questionnaire` — fill-blanks / flag-conflicts / never overwrite; `list_provenance` case-scoped read; `link_verification` — party ↔ identity/screening check, state derived from the check); `aml-finance` `duplicate_document_refs` surfaced in workspace | `OwnershipControlTab` (entity summary, owners/reps tables, completeness + PEP/sanctions warnings, conflicts panel, import action, visual ownership graph + accessible tables, per-party check linking); workspace Documents & Evidence evidence-reference list + reused-document scan | `amlPortalContracts.test.ts` (Phase 6 blocks: write-role gate, no-silent-overwrite, provenance attribution, idempotency, portal isolation, derived verification state) |
| Finance reconciliation: request loop, canonical submissions, finance-safe channel (V2 directive Phase 7) | V2-7 | `aml.finance_requests` (new, RLS deny-by-default), `aml.finance_comparisons`, `aml.finance_discrepancies`, `aml.evidence_references`; `aml.cases.finance_portal_status` dimension | `aml-finance` (`create/list/review/resolve_finance_request`, dimension transitions per §15.3, shared engine import); `finance-portal-aml-requests` (new, finance-session auth, assignment-scoped, finance-safe projection; submissions become comparisons via `_shared/amlFinanceEngine.ts`) | Staff: `AmlFinance` Requests tab + "Ask finance" on discrepancies, workspace `FundingFinanceTab` request counts. Broker: `AmlFinanceRequestsCard` on the purchase-file page (funding form, clarification/evidence responses) | `amlPortalContracts.test.ts` (Phase 7 blocks: write gates, §15.3 transitions, deny-by-default table, no case-id/risk/discrepancy leakage, shared engine single-source) |
| Risk, decision and service gate: recommendation → decision → explicit gate (V2 directive Phase 8) | V2-8 | `aml.service_gate_decisions` (new, C.4 record, browser read-only), `aml.analyst_recommendations` (new), `aml.risk_overrides` + `evidence_note`/`program_version`; `aml.cases.service_gate_status/_effective_at/_policy_version` | `aml-risk` (`recommend`, `list_recommendations`, `set_service_gate` — sole gate writer with approval preconditions, `gate_contract` C.4 read, `recalc_status` staleness; override evidence + policy stamping; `decide` actions pending recommendations) | `RiskTab`: staleness banner with recompute, analyst recommendation card, reviewer decision panel, service-gate panel (C.4 display + reasoned change, MLRO-only lock/terminate) | `amlPortalContracts.test.ts` (Phase 8 block: override evidence rule, gate preconditions, §16 separation — evaluate never writes the gate, C.4 fields, recalculation reasons, read-only tables) |
| Commercial visual refinement: accessible action dialogs, labelling, density (V2 directive Phase 12) | V2-12 | n/a (presentation only) | n/a | `usePromptDialog` (labelled fields, inline validation mirroring server rules, aria-invalid/describedby, role=alert, focus-first-error) replacing 8 `window.prompt` call sites in `AmlCaseWorkspace`; accessible names on request subject / client message / status reason; ellipsis placeholders; tabular numerals on ownership figures | Build + lint + tsc clean; Web Interface Guidelines review; documented in docs/aml/production-readiness.md §6 |
| UAT and controlled rollout: regression, rollback rehearsal, readiness (V2 directive Phase 13) | V2-13 | all programme tables (rollback rehearsed live in an aborted transaction) | all nine deployed AML functions byte-verified | Staged feature-flag enablement order; production-readiness report | `docs/aml/production-readiness.md` (112/112 AML tests, build, security:static 511 files, edd-boundary, inventory; rollback rehearsal; flag state) |
| Regulatory and records: trigger-based retention, disposal controls, audit integrity (V2 directive Phase 11) | V2-11 | `aml.retention_triggers` (new, browser read-only), `aml.retention_scan_items` + trigger/basis/minimum-date/privacy/dependency/evidence columns; existing `aml.reports`, `aml.legal_holds`, `aml.retention_scans` | `aml-records` (`record_retention_trigger`, `sync_case_triggers`, `list_retention_records`; `dry_run_scan` rewritten to select on recorded triggers' minimum retention dates; dependency + hold + trigger re-checks at execution; disposal evidence; `verify_audit_chain`, `export_audit_bundle` MLRO-only) | Records & Privacy retention register (§18 display contract); audit-integrity verification and case audit-bundle export | `amlPortalContracts.test.ts` (Phase 11 block: no age-based disposal, trigger catalogue + legal basis, dependency/hold/trigger checks, truthful disposal evidence, chain recomputation, MLRO-only reasoned export, read-only table) |
| Monitoring and ongoing CDD: risk-based cycle, trigger reviews, deadlines, relationship end (V2 directive Phase 10) | V2-10 | `aml.cases` + `monitoring_status`/`relationship_ended_at`/`_reason`/`_recorded_by`/`next_periodic_review_at`/`last_periodic_review_at`; `aml.existing_customer_reviews` + `trigger_kind`/`trigger_event_id`/`original_due_at`/`extension_count`/`extension_reason`; `aml.tenant_settings.review_interval_config` | `aml-monitoring` (`schedule_periodic_review`, `record_trigger_review`, `assign_review`, `extend_review_deadline`, `end_relationship`, `set_monitoring_status`, `case_monitoring_summary`; `complete_review` books the next cycle; cron raises due periodic reviews and skips ended relationships) | Workspace **Monitoring & Reviews** section (relationship state, review cycle + next review, screening-refresh due, open reviews with take/extend/complete, trigger-review form, open alerts + EDD rollup) | `amlPortalContracts.test.ts` (Phase 10 block: risk-based scheduling, trigger catalogue + detail rule, deadline-extension provenance, relationship-end gating and preservation, ended-relationship scan exclusion, MLRO-only reinstatement, additive migration) |
| Transaction and counterparty: delayed CDD + uncooperative record + case workspace surface (V2 directive Phase 9) | V2-9 | `aml.counterparty_cases` + `delayed_cdd_deadline`/`delayed_cdd_justification`/`uncooperative*` (additive); existing `aml.transactions`, `aml.transaction_obligations`, `aml.counterparty_requests`/`_attempts` | `aml-transactions` (`set_delayed_cdd`, `mark_uncooperative` — requires ≥2 recorded attempts as reasonable-steps evidence; generic `upsert_cp_case` strips the controlled fields; counterparty actions append hash-chained case events) | Workspace `PurchaseCounterpartySection` (purchase-file linkage, settlement gate, transactions with settlement-date change display, counterparty CDD list with delayed-CDD/uncooperative badges + actions, open obligations) | `amlPortalContracts.test.ts` (Phase 9 block: reasonable-steps rule, dated-deadline rule, controlled-field stripping, case-timeline audit) |

Update this table whenever an AML surface adds or removes a table, edge function, or scenario.
