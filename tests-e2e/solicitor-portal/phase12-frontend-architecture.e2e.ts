import { test, expect } from '@playwright/test';
test.describe('Solicitor workspace accessibility',()=>{test.skip(!process.env.SOLICITOR_E2E_EMAIL,'requires deployed solicitor fixture');test('nested navigation is keyboard and mobile usable',async({page})=>{await page.setViewportSize({width:390,height:844});await page.goto('/solicitor/login');await page.getByLabel(/email/i).fill(process.env.SOLICITOR_E2E_EMAIL!);await page.getByLabel(/password/i).fill(process.env.SOLICITOR_E2E_PASSWORD!);await page.getByRole('button',{name:/sign in/i}).click();await expect(page.getByRole('navigation',{name:'Solicitor portal'})).toBeVisible();await page.keyboard.press('Tab');await expect(page.locator(':focus')).toBeVisible();await page.getByRole('link',{name:'Matters'}).click();await expect(page.getByRole('heading',{name:'Matters'})).toBeVisible();await expect(page.getByLabel('Search matters')).toBeVisible()})});
