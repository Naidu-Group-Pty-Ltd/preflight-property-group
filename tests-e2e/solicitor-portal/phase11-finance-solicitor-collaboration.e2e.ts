import { expect,test } from '@playwright/test';import { readFileSync } from 'node:fs';
test('Phase 11 collaboration surfaces retain privacy labels and accessible controls',async()=>{const finance=readFileSync('src/components/finance-portal/FinanceLegalCoordinationPanel.tsx','utf8'),solicitor=readFileSync('src/components/solicitor-portal/MatterFinanceCoordinationPanel.tsx','utf8'),admin=readFileSync('src/components/admin/solicitor-portal/TransactionCasesPanel.tsx','utf8');expect(finance).toContain('aria-label="Message to legal team"');expect(finance).toContain('Private legal notes are never shown');expect(solicitor).toContain('Financial-position and AML data are never shown');expect(admin).toContain('Queue projection refresh');});
