/**
 * Bridge — the implementation lives with the Edge Functions.
 *
 * A design system is read identically by the form that creates one and the
 * route that renders under one, which is the whole point of a single reader.
 */
export * from '../../../supabase/functions/_shared/brandDesign/system.pure.ts';
