/**
 * Bridge — the implementation lives with the Edge Functions.
 *
 * One brand colour grown into the family of colours a drawn document needs.
 * Nothing may be added here; see `__tests__/designSystemSourceOfTruth.spec.ts`.
 */
export * from '../../../supabase/functions/_shared/reportDesign/brandFamily.pure.ts';
