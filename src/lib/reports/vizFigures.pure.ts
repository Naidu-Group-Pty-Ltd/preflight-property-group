/**
 * Bridge — the implementation lives with the Edge Functions.
 *
 * The drawing half of the chart vocabulary; the parsing half is
 * `vizDirectives.pure.ts`.
 */
export * from '../../../supabase/functions/_shared/reports/vizFigures.pure.ts';
