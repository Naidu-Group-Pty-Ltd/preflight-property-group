// Bridge — the implementation lives with the Edge Functions, so one module
// serves the scorer and the surfaces that render its decision.
export * from '../../../../supabase/functions/_shared/reports/market/scoringInputPolicy.pure.ts';
