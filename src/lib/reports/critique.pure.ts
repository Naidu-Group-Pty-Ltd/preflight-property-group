/**
 * Bridge — the implementation lives with the Edge Functions.
 *
 * The rubric is run from a script and from specs on this side, and could be run
 * from a render route on the other. One implementation, two import paths.
 */
export * from '../../../supabase/functions/_shared/reports/critique.pure.ts';
