/**
 * Bridge — the implementation lives with the Edge Functions, because the
 * generator banks the subject facts every continuation reads back.
 *
 * See `supabase/functions/_shared/reports/investment/subjectFacts.pure.ts`.
 */
export * from '../../../../supabase/functions/_shared/reports/investment/subjectFacts.pure.ts';
