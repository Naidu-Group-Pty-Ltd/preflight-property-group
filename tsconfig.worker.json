{
  "//": "The Builder Stock Cloudflare worker, typechecked on its own: it ships outside src/ and supabase/functions/, deliberately imports nothing, and types its AI binding locally — so the app tsconfig never sees it and vite never checks it. Strict, DOM-lib (workerd exposes the same fetch/streams/crypto surface), no emit.",
  "//exclude": "builder-stock-pdf-worker breaks the premise above ON PURPOSE: running the SHARED election is the whole point of it, so it imports `supabase/functions/_shared/builderStock/` and therefore carries Deno-style `.ts` specifiers and a remote `https://esm.sh/...` import that a `moduleResolution: bundler` compiler cannot resolve — 70 errors, none of which is a defect. Relaxing this config to accept them would weaken the gate for the worker it was written for, which imports nothing. So that worker is validated instead by the two toolchains that actually compile it — `deno check`, which understands both, and the wrangler build, which resolves the alias — in the `builder-stock-pdf-worker` CI job. It is not exempt from validation; it is validated differently because it is compiled differently. The exclusion names that one directory so it can never quietly grow to cover the image worker this file exists for.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ES2022",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true
  },
  "include": ["cloudflare"],
  "exclude": ["cloudflare/builder-stock-pdf-worker"]
}
